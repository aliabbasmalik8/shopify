<h1>Editing Product</h1>

<%= render 'form', product: @product %>

<%= link_to 'Show', edit_shop_product_path(@product.shop_id,@product.id) %> |
<%= link_to 'Back', shop_products_path(@product.shop_id) %>
